# 基础科学计算 (保留原有的)
numpy
scipy
scikit-learn
pandas
matplotlib
IPython
jupyterlab
six

# 深度学习核心 (已确认支持CUDA)
torch
torchvision
torchaudio

# 符号回归常用库 (Windows友好，即装即用)
pysr          # 经典库，用演化算法找公式
# 如果要跑 TPSR 这种基于 Transformer 的模型，直接 git clone 即可，不强制放这里